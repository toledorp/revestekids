<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Perfil - Reveste Kids</title>
    <link rel="stylesheet" href="/css/sidebar.css"> 
    <link rel="stylesheet" href="/css/perfil.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
<div class="app-layout">
    <%- include('partials/_sidebar') %>
    <main class="content-area">
        <div class="content-area-pad">
            <div class="perfil-container">
                <h1>Meu Perfil</h1>
                <div class="foto-perfil" style="text-align: center; margin-bottom: 20px;">
                    <% if (usuario.foto_perfil) { %>
                        <img src="/uploads/perfis/<%= usuario.foto_perfil %>" alt="Foto de Perfil" class="foto-perfil-img">
                    <% } else { %>
                        <img src="/img/default-user.png" alt="Foto Padrão" class="foto-perfil-img">
                    <% } %>
                </div>
                <div class="secao">
                    <h2>Informações Pessoais</h2>
                    <p><strong>Nome:</strong> <%= usuario.nome %> <%= usuario.sobrenome %></p>
                    <p><strong>E-mail:</strong> <%= usuario.email %></p>
                </div>
                <div class="secao">
                    <h2>Endereço</h2>
                    <p><strong>CEP:</strong> <%= usuario.cep %></p>
                    <p><strong>Logradouro:</strong> <%= usuario.logradouro %>, nº <%= usuario.numero %></p>
                    <p><strong>Bairro:</strong> <%= usuario.bairro %></p>
                    <p><strong>Cidade:</strong> <%= usuario.cidade %> - <%= usuario.estado %></p>
                </div>

                <!-- Nova seção para centralizar o botão de Edição -->
                <div class="secao">
                    <a href="/perfil/editar" class="botao"><i class="fas fa-edit"></i> Editar meu perfil</a>
                    <a href="/perfil/apagar" class="link-apagar" onclick="return confirm('Tem certeza que deseja apagar sua conta?')">
                        <i class="fas fa-trash-alt"></i> Apagar Conta
                    </a>
                </div>
                
               
            </div>
        </div>
    </main>
</div>
</body>
</html>