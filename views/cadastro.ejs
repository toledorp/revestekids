<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro</title>
    <link href="https://fonts.googleapis.com/css2?family=Barrio&family=Inter:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="/css/cadastro.css" rel="stylesheet">
</head>
<body class="cadastro-body">
    <header class="main-header">
        <div class="logo-title-wrapper">
            <img src="/imgs/logo.jpg" alt="ReVeste Kids Logo" class="cadastro-logo">
            <h1 class="cadastro-title">ReVeste Kids</h1>
        </div>
        </header>
    <% if (messages && Object.keys(messages).length > 0) { %>
        <div class="message-container">
            <% if (messages.error) { %>
                <% messages.error.forEach(function(msg) { %>
                    <div class="alert alert-danger">
                        <%= msg %>
                    </div>
                <% }); %>
            <% } %>
            <% if (messages.success) { %>
                <% messages.success.forEach(function(msg) { %>
                    <div class="alert alert-success">
                        <%= msg %>
                    </div>
                <% }); %>
            <% } %>
            <% if (messages.warning) { %>
                <% messages.warning.forEach(function(msg) { %>
                    <div class="alert alert-warning">
                        <%= msg %>
                    </div>
                <% }); %>
            <% } %>
        </div>
    <% } %>
    <div class="cadastro-container">
        <form action="/cadastro" method="POST" class="cadastro-form" enctype="multipart/form-data">
            <div class="grid-2-cols">
                <div class="input-group">
                    <label for="nome" class="input-label">Nome</label>
                    <input type="text" id="nome" name="nome" placeholder="Nome" class="text-input" required>
                </div>
                <div class="input-group">
                    <label for="sobrenome" class="input-label">Sobrenome</label>
                    <input type="text" id="sobrenome" name="sobrenome" placeholder="Sobrenome"
                        class="text-input" required>
                </div>
            </div>
            <div class="input-group foto-cadastro">
                <label for="foto_perfil_cadastro" class="label-file">
                    <i class="fas fa-camera"></i> Foto de Perfil (Opcional)
                </label>
                <input type="file" id="foto_perfil_cadastro" name="foto_perfil_cadastro" accept="image/*" class="file-input-hidden">
                <p class="file-info" id="file-name-display">Nenhum arquivo selecionado.</p>
            </div>
            <div class="input-group">
                <label for="email" class="input-label">E-mail</label>
                <input type="email" id="email" name="email" placeholder="seu@email.com" class="text-input"
                    required>
            </div>
            <div class="grid-2-cols">
                <div class="input-group"> 
                    <label for="senha" class="input-label">Senha</label>
                    <input type="password" id="senha" name="senha" placeholder="Senha"
                        class="text-input password-input" required minlength="6">
                </div>
                <div class="input-group"> 
                    <label for="confirmacao_senha" class="input-label">Confirmação de Senha</label>
                    <input type="password" id="confirmacao_senha" name="confirmacao_senha"
                        placeholder="Confirme sua senha" class="text-input password-input" required
                        minlength="6">
                </div>
            </div>
            <h3 class="section-title">Endereço</h3>
            <div class="grid-2-cols">
                <div class="input-group"> 
                    <label for="cep" class="input-label">CEP</label>
                    <input type="text" id="cep" name="cep" placeholder="00000-000" class="text-input" required>
                </div>
                <div class="input-group">
                    <label for="numero" class="input-label">Número</label>
                    <input type="text" id="numero" name="numero" placeholder="Número" class="text-input"
                        required>
                </div>
            </div>
            <div class="input-group">
                <label for="logradouro" class="input-label">Logradouro</label>
                <input type="text" id="logradouro" name="logradouro" placeholder="Logradouro" class="text-input"
                    required>
            </div>
            <div class="grid-2-cols">
                <div class="input-group">
                    <label for="bairro" class="input-label">Bairro</label>
                    <input type="text" id="bairro" name="bairro" placeholder="Bairro" class="text-input"
                        required>
                </div>
                <div class="input-group">
                    <label for="cidade" class="input-label">Cidade</label>
                    <input type="text" id="cidade" name="cidade" placeholder="Cidade" class="text-input"
                        required>
                </div>
            </div>
            <div class="input-group">
                <label for="estado" class="input-label">Estado (UF)</label>
                <input type="text" id="estado" name="estado" placeholder="Estado (UF)" class="text-input"
                    required maxlength="2">
            </div>
            <button type="submit" class="btn-cadastro">
                Criar conta
            </button>
        </form>
        <div class="login-link-container">
            <p>Já tem uma conta? <a href="/login" class="login-link">Fazer login</a></p>
        </div>
    </div>

    <script src="/js/cadastro.js"></script>
</body>
</html>