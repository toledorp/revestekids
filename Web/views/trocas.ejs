<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciamento de Trocas</title>
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/trocas.css"> 
</head>
<body>  
    <div class="app-layout">
        <%- include('partials/_sidebar') %>
        <main class="content-area">
            <div class="content-area-pad">          
                <h2 class="page-title">Gerenciamento de Trocas</h2>
                <div class="tabs-navigation">                   
                    <button class="tab-button active" data-tab="recebidas"> Propostas Recebidas
                        <span class="badge badge-recebidas"><%= propostasRecebidas.length %></span>
                    </button>
                    <button class="tab-button" data-tab="enviadas">
                        Propostas Enviadas
                        <span class="badge badge-enviadas"><%= propostasEnviadas.length %></span>
                    </button>
                    <button class="tab-button" data-tab="historico">
                        Histórico
                        <span class="badge badge-historico"><%= historicoTrocas.length %></span>
                    </button>
                </div>               
                <div class="tab-content-area">                   
                    <div id="recebidas" class="tab-pane proposta-list active-tab-pane"> <% if (propostasRecebidas.length > 0) { %>
                            <% propostasRecebidas.forEach(proposta => { %>
                                <%- include('partials/troca_list_card', { 
                                    item: proposta, 
                                    tipo: 'recebida', 
                                    userId: userId,
                                    dataAcao: proposta.dataProposta 
                                }) %>
                            <% }); %>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fas fa-inbox fa-3x icon-principal"></i>
                                <h3>Caixa de entrada limpa!</h3>
                                <p>Nenhuma proposta de troca recebida no momento. Volte ao <a href="/feed">Feed</a> para encontrar peças.</p>
                            </div>
                        <% } %>
                    </div>
                    <div id="enviadas" class="tab-pane proposta-list" style="display: none;"> <% if (propostasEnviadas.length > 0) { %>
                            <% propostasEnviadas.forEach(proposta => { %>
                                <%- include('partials/troca_list_card', { 
                                    item: proposta, 
                                    tipo: 'enviada', 
                                    userId: userId,
                                    dataAcao: proposta.dataProposta 
                                }) %>
                            <% }); %>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fas fa-paper-plane fa-3x icon-principal"></i>
                                <h3>Nenhuma Proposta Enviada Ativa</h3>
                                <p>Você pode ver as peças disponíveis no <a href="/feed">Feed</a> e enviar sua primeira proposta!</p>
                            </div>
                        <% } %>
                    </div>
                    <div id="historico" class="tab-pane proposta-list" style="display: none;"> <% if (historicoTrocas.length > 0) { %>
                            <% historicoTrocas.forEach(troca => { %>
                                <%- include('partials/troca_list_card', { 
                                    item: troca, 
                                    tipo: 'historico', 
                                    userId: userId,
                                    dataAcao: troca.dataFinalizacao
                                }) %>
                            <% }); %>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fas fa-history fa-3x icon-principal"></i>
                                <h3>Histórico Vazio</h3>
                                <p>As trocas finalizadas aparecerão aqui.</p>
                            </div>
                        <% } %>
                    </div>

                </div>
            </div>
        </main>
    </div>
    <div id="trocaModal" class="modal-overlay" style="display: none;">
        <div class="modal-content">
            <span class="close-button">&times;</span>
            <div id="modalBodyContent">
                <p>Carregando detalhes da troca...</p>
            </div>
        </div>
    </div>
    
    <script src="/js/trocas.js" defer></script> </body>
</html>